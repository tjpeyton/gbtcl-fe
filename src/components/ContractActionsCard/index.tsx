
import { ContractDocument } from '@/lib/types/contract'

type ContractActionsCardProps = {
  contract: ContractDocument
}

const ContractActionsCard = ({ contract }: ContractActionsCardProps) => {
  return <div>ContractActionsCard</div>
}

export default ContractActionsCard
  